const bfsRecursive = function() {
  if (node === null) return;
  
  // Enqueue src and mark it as visited

  // while queue is not empty
    // Deque a node and visit
    // For each unvisited neighbor
      // Enqueue it 
      // mark it as visited

  // When adding to queue is when we check if visited

  // So what is the recursive task?

  // We don't work in the nodes themselves but on the queue
}